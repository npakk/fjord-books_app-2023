<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.edit.title'), edit_book_path(@book) %> |
  <%= link_to t('view.back', title: t('books.index.title')), books_path %>

  <%= button_to t('view.destroy', title: Book.model_name.human), @book, method: :delete %>
</nav>
