<div id="<%= dom_id report %>">
  <p>
    <strong><%= Report.human_attribute_name(:user_id) %>:</strong>
    <%= report.user.name.to_s.blank? ? report.user.email : report.user.name %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:title) %>:</strong>
    <%= report.title %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:body) %>:</strong>
    <%= report.body %>
  </p>

</div>
