<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render 'comments/form', commentable: @commentable, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.cancel', name: i18n_pluralize(@commentable.model_name.human.downcase)), @commentable %>
</nav>
